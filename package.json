{
  "name": "tetrix",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild",
    "dev": "node esbuild --dev",
    "lint": "biome check --fix",
    "format": "biome format --write",
    "preview": "npx esbuild --servedir=public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Valexr/Tetrix"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "esbuild": "^0.28.1",
    "esbuild-svelte": "^0.9.5",
    "svelte": "^5.56.4",
    "typescript": "^7.0.2"
  }
}
